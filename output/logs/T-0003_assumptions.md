# Assumptions for T-0003: Generate Per-Task Prompt Template

- The `solve.md` file provided initially contains the complete and correct definition for §3 (Per-Task Prompt Template).
- The template placeholders (`{{...}}`) should be preserved exactly as they appear in the source.
- The formatting required for the final prompt is standard Markdown, including the code block syntax.

## Plan Justification

The chosen method is "Extract and reformat §3 from solve.md". This is the most direct approach as the source content is explicitly provided in the specified file (`solve.md`). Simple text extraction and ensuring the template format (including placeholders and code block) is maintained are sufficient.